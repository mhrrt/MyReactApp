buildscript {
    ext {
        buildToolsVersion = "35.0.0"
        minSdkVersion = 24
        compileSdkVersion = 35
        targetSdkVersion = 34
        ndkVersion = "27.1.12297006"
        kotlinVersion = "2.0.21"
    }
    repositories {
        google()
        mavenCentral()
        // maven { url 'https://artifacts.applovin.com/android' } // Apollvin max 12mar
    }
    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
        // classpath("com.google.android.gms:play-services-ads:22.6.0") // Ensure this is updated 10Mar
        // classpath "com.applovin.quality:AppLovinQualityServiceGradlePlugin:+" // Apollvin max 12mar
    }
}

apply plugin: "com.facebook.react.rootproject"
